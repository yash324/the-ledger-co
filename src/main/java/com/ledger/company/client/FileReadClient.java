package com.ledger.company.client;

import java.io.BufferedReader;

public class FileReadClient extends ReadClient {
    public FileReadClient(BufferedReader inputReader) {
        super(inputReader);
    }
}
